export const environment = {
    production: false,
    keycloak: {
        realm: 'your-realm',
        authServerUrl: 'http://localhost:8180/auth',
        clientId: 'your-client-id'
    }
};
